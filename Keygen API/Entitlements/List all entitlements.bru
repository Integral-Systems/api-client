meta {
  name: List all entitlements
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/accounts/{{account}}/entitlements
  body: none
  auth: bearer
}

params:query {
  ~page: 
  ~limit: 
}

auth:bearer {
  token: {{token}}
}
