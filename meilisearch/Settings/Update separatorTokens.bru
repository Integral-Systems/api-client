meta {
  name: Update separatorTokens
  type: http
  seq: 50
}

put {
  url: {{baseUrl}}/indexes/:indexUid/settings/separator-tokens
  body: json
  auth: bearer
}

params:path {
  indexUid: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  [
    {}
  ]
}
