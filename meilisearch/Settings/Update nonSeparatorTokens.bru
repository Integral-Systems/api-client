meta {
  name: Update nonSeparatorTokens
  type: http
  seq: 29
}

put {
  url: {{baseUrl}}/indexes/:indexUid/settings/non-separator-tokens
  body: json
  auth: bearer
}

params:path {
  indexUid: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  [
    {}
  ]
}
