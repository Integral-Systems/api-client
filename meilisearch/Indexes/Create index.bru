meta {
  name: Create index
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/indexes
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "uid": "",
    "primaryKey": ""
  }
}
